- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc:
      if: '[ `uname` = Darwin ]'
      path: zshrc
    ~/.zshrc:
      if: '[ `uname` = Linux ]'
      path: zshrc-linux
    ~/.config/alacritty.yml: alacritty
